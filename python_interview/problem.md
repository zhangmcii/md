
视频标注平台：


总结：
这个项目整体实现不复杂，但有几个比较实际的难点。
一个是数据结构的问题，原始数据是嵌套的 JSON，但页面编辑和导出结构不一致，比如导出是按分镜组拆分的，所以需要做一层数据重组，这里处理起来比较容易出错。
第二个是编辑和状态的联动，比如数据标记为“已确认”后，如果用户再修改，需要自动回退状态，这里需要统一处理数据变更，而不是在每个字段里写逻辑。
另外一个是防止数据丢失，因为标注过程比较长，我加了本地缓存，避免刷新后数据丢失。
还有一个考虑是没有把系统做复杂，比如没有引入数据库，而是保持后端轻量，这其实也是一个取舍。

<details>
<summary>每点解释</summary>

1.复杂 JSON 结构的编辑与导出一致性（最核心）
这个项目的数据结构是嵌套的 JSONL，一个视频下面有分镜组，分镜组下面还有分镜。
页面上是按层级展示和编辑的，但最终需要导出成指定格式，而且导出结构和原始数据不完全一致。

你做了什么（关键）
我在对数据做了一层转换，把前端修改后的数据重新组装成以“分镜组”为单位的 JSON，再逐条输出为 JSONL。

trade-off（加分点）
这样做的好处是导出格式更符合下游使用，但缺点是需要维护一套额外的数据转换逻辑。

2.行内编辑 + 状态管理的联动

可以这样说
页面支持行内编辑，并且有“已确认/待审核”的状态，如果用户在已确认状态下修改数据，需要自动回退状态。

难在哪？
编辑行为 → 状态变化（自动触发）

可以这样讲
我在设计时，把“编辑行为”和“状态管理”做了解耦，通过监听数据变更统一处理状态回退，避免在每个字段里单独处理。

3.标注是一个耗时过程，如果刷新页面会丢数据，所以加了本地缓存做草稿恢复。

4.（可选加分）：系统复杂度控制
这个项目一开始很容易往复杂方向走，比如引入数据库、多用户角色权限、版本控制等，但实际需求是内部工具，所以难点反而在于控制复杂度，如何设计一个简单但可用的文件管理方案。。

你做了什么
后端只保留文件上传和读取能力，把标注逻辑放在前端，保证系统足够轻量。

是一个在功能完整性和实现成本之间的取舍。




另外：多人编辑下的数据覆盖问题

怎么说（直接可用）
这个系统虽然不是实时协作，但存在多人接力编辑的场景。最大的问题是：用户可能基于旧数据修改，提交时覆盖掉别人最新结果。

你做了什么
我当时没有引入复杂的协同算法，而是做了一个轻量方案：在数据中增加更新时间字段，提交时做校验，如果版本落后就提示用户。

trade-off（关键加分点）
这个方案的好处是实现简单、成本低，但缺点是不能完全避免冲突，只是降低概率。当时考虑到系统使用人数不多，这个方案是更合适的。
</details>




内容平台：

1. 实时通信与状态一致性 (Socket.IO + Vuex/Pinia)
潜在难点： 即使你觉得 Socket.IO 用起来很简单，但如何在高频社交场景下保证前端状态与后端数据的一致性是有深度的。

你可以这样说： “项目集成了实时聊天功能。难点在于如何处理网络波动导致的 Socket 断连与重连。如果直接重连，可能会丢失断线期间的消息。我研究并实现了心跳机制（Heartbeat）和消息确认（ACK）机制，确保消息不丢失。同时，在前端我需要将 Socket 推送的数据实时合并到本地的 Pinia 状态中，并处理好消息去重，防止因为网络抖动导致同一条消息显示两次。”

2. 鉴权体系的安全性与统一 (OAuth + JWT + Socket)
潜在难点： 你的项目有 OAuth 登录。如何让 HTTP 请求和 WebSocket 连接共享一套鉴权逻辑？

你可以这样说： “项目中使用了 OAuth 第三方登录和 JWT。难点在于长连接（Socket.IO）的权限校验。普通的 API 请求可以在 Header 带 Token，但 WebSocket 的握手阶段比较特殊。我需要实现在 Socket 建立连接时进行中间件校验，并在 Token 过期后，如何平滑地让长连接也完成无感刷新，而不是直接把用户踢下线。”

3. 移动端优先的体验与“优雅降级” (Graceful Degradation)
潜在难点： 你的 README 提到了“优雅降级”，这本身就是一个非常高级的面试话题。

你可以这样说： “作为一个移动端优先的项目，我重点处理了复杂交互在低端机型或弱网环境下的表现（优雅降级）。例如，在网络极差导致 Socket 无法连接时，我设计了一套降级方案，让实时聊天功能回退到短轮询（Short Polling）或者至少保证基础的博客浏览不受影响。此外，针对移动端的视口抖动、键盘遮挡输入框等细节，我也做了深度适配，这比单纯实现功能要花更多精力去调试。”

4. 数据模型与性能考量 (Flask + SQLAlchemy)
潜在难点： 社交项目涉及“用户-文章-评论-点赞”这种多对多的复杂关系。

你可以这样说： “虽然功能写完了，但在设计数据库模型时，我考虑到了社交关系的查询效率。比如‘点赞’和‘关注’，如果直接 Join 多个大表，性能会随着数据量增加而下降。我通过优化索引，以及在某些地方采用‘空间换时间’的思路（冗余字段）来减少复杂查询。此外，针对 Flask 这种同步框架处理异步 Socket 的并发压力，我通过引入 Eventlet 协程库进行了优化，保证了服务器在高并发下的稳定性。”



提问：
项目理解与整体设计（意图与边界）
## 解耦设计：
1. 你的项目中既有传统的 RESTful API，又有基于 Socket.IO 的长连接。请详细说明你的后端是如何划分这两个模块的？如果即时通讯服务（IM）因为消息量过载而崩溃，是否会影响到用户的博客浏览和登录功能？

2. 现有的开源博客或社交平台（如 WordPress, Typecho, 或者各种轻量级社交框架）已经非常成熟，你的项目解决了它们中哪一个“无法满足”的核心问题？或者说，这个项目的“灵魂功能”在技术实现上有何独特之处？

## 技术深挖（并发与实时性）
WebSocket 扩展性： 
Flask 本身是 WSGI 协议，它是同步的。你在实现 Socket.IO 时，底层使用的是线程模式还是协程（如 Gevent/Eventlet）？如果现在有 5000 个用户同时在线保持长连接，你的服务器内存和 CPU 占用大概在什么水平？你如何监控并定位其中的死锁或资源泄露问题？

状态同步一致性： 
在社交场景下，如果 A 用户点赞了 B 用户的文章，此时 B 正在通过 WebSocket 聊天。你是如何保证点赞动态能即时、准确地推送到 B 的界面，同时保证数据库里的计数和前端显示的数字在网络波动时依然保持最终一致性？

身份校验的成本： 
你的项目使用了 JWT 和 OAuth。对于 WebSocket 连接，你是在握手阶段校验一次权限，还是在每次消息传输时都进行校验？如果是前者，如何处理 Token 在连接期间过期的情况？如果是后者，如何在高频通信中降低鉴权带来的性能损耗？

## 数据建模与存储（深度与优化）
关系模型性能： 
社交系统最核心的是“关系”（如：粉丝、关注、好友）。在你的数据库设计中，如果一个“大 V”拥有 100 万粉丝，当你查询他的粉丝列表或者判断某个用户是否是他的粉丝时，你的 SQL 语句是如何优化的？有没有考虑到深度分页带来的性能坍塌？

复杂嵌套处理： 
博客系统必然涉及“多级评论”或“嵌套回复”。你是采用递归查询（Recursive Query）、路径枚举（Path Enumeration）还是闭包表（Closure Table）来存储这种树形结构？请从查询效率和写入复杂度的角度对比说明你的选择理由。

索引失效场景： 
请列举你项目中搜索功能（如搜索文章标题或内容）的实现逻辑。在模糊匹配 LIKE '%keyword%' 的情况下，索引会失效，你当时是如何处理大文本搜索的？有没有考虑过引入全文检索引擎？

## 系统设计能力（高可用与扩展）
异步任务治理： 
诸如“发送注册邮件”、“消息通知推送”这些耗时操作，如果你依然放在请求响应循环（Request-Response Cycle）里处理，会严重拖慢用户体验。你是否设计了任务队列？如何保证任务在处理失败后的重试逻辑和幂等性？

## 工程能力与进阶思考（批判性思维）
代码腐化与重构： 
现在回看你的源代码，哪一个模块的设计让你觉得“当时写得太耦合了，现在很难维护”？如果让你把“即时聊天”模块彻底抽离成一个独立的微服务，你认为最大的技术障碍在哪里？

安全性防御： 
除了基础的 JWT，你如何防御针对社交平台的 XSS 攻击（尤其是在用户评论和个人简介处）？对于恶意的 API 爬虫，你有没有在后端实现频率限制（Rate Limiting）？

商业化考量： 
如果这个系统要正式商业化运行，且需要支持“万人同时在线聊天”，你认为目前代码中最大的性能“定时炸弹”在哪里？你会选择更换哪一部分的技术栈来彻底解决它？




我原来的启动脚本
```aiignore
...
exec gunicorn -b :5000 --worker-class eventlet -w 4 --access-logfile - --error-logfile - flasky:app
```
原来的gunicorn同时处理http和socketio请求
因为eventlet专为异步设计，而我的接口中包含大量同步操作，比如查数据库，ORM 会阻塞整个 greenlet，并且eventlet 对 CPU 任务极其不友好。

后来对服务进行了拆分，主 API (HTTP)与Socket.IO 服务分离，
让同步 Worker (`-w 8`)，处理传统的 CRUD 请求，
而让Socket.IO 服务保持使用 Eventlet，专门处理即时通讯

```
        ┌──────────────┐
        │  Nginx       │
        │              │
        ├──── HTTP ────┼──▶ gunicorn (sync workers)
        │              │
        └─ WebSocket ──┼──▶ gunicorn (eventlet/gevent)
                       └── Redis (message_queue)
```

RESTful API都集中在api蓝图下，接口的url中需要携带api前缀进行访问。
而Socket.IO并不走普通的HTTP请求，事件集中放在一个单独的模块中（包括连接，断开，进入私信页事件）。
RESTful API所在的蓝图在Flask应用工厂函数中注册到应用实例中。
Socket.IO服务最终都在webSocket应用工厂函数中注册到应用实例中。

所以当实时消息量过载而崩溃，只会影响到WebSocket 服务， 不会影响到用户的博客浏览和登录功能？



WebSocket 扩展性： 
实现Socket.Io时，低层使用的是协程Eventlet。

状态同步一致性： 
B正在聊天，它已经建立了websocket长连接，那么A点赞时，就会触发websocket消息推送，他会服用已有的连接会话推送过去。所有的消息都按顺序提交到celery任务队列中，celery按顺序执行，只要B用户在线正处入前端页面，所以可以保证在可控的延时或者及时、准确的推送到B的界面。

消息的推送和将消息写入数据库是包含在同一个celery任务中。
当A用户点赞后，前端提示点赞成功的样式，就表明计数已正确写入数据库中，前端的显示的数字一定是正确的。由于点赞而产生的消息推送是在celery异步执行，如果存在celery服务异常或者网络波动导致的任务没被执行或者判定接受者不在线，那么系统不会保证消息会被接受到。

最坏的情况是，celery服务异常，点赞产生的通知消息可能没有被记录到数据库，也未触发任何websocket通知， 那么B用户无法实时接收消息，也无法通过刷新页面来接受到被点赞通知。

；好一点的情况下，celery正常，websocket异常，那么通知消息记录会记录到数据库，但是B用户无法实时接收到消息，但B用户刷新页面时，会重新从通知表中拉取未读的消息，最终也能保持一致性。

总的来说，点赞技术一定会被记录，要么点赞失败，要么成功。但点赞所产生的消息取决于celery服务和websocket服务是否正常。在服务都正常的情况下，数据库里的计数和前端显示的数字在网络波动时依然保持最终一致性。



身份校验的成本： 
客户端主动触发的任何websocket事件，在消息传输时都会进行校验。
服务器在鉴权的过程中发现Token过期，jwt校验过期的token会产生异常，代码会捕捉ExpiredSignatureError（ 签名过期异常），捕捉后会抛出新的异常，此异常会直接响应给前端。
前端会捕捉此异常并提示用户重新登陆或者刷新token有效，会无感刷新token，再重新执行此请求。


关系模型性能：
粉丝id加了索引，sql语句按分页查询。

复杂嵌套处理：
多级评论是递归查询。评论表设置了根评论和直接孩子评论和直接父评论。查询某篇文章的评论时，先查询该篇文章下所有的根评论，再遍历查找每个根评论下的所有回复。
回复评论都会有一个直接父评论和直接孩子评论，最终在前端按指定的格式渲染层级。

总的老说，多级评论的查询是按2层查找。第一次查找所有的根评论，第2层查找每个根评论下的所有回复。

这样查询很方便，直接提供根评论id就可以一次性查找，不拥递归查询。


索引失效场景：
首页根据关键词搜索相关文章，使用的是mysql的全文检索引擎

异步任务治理：
确实有使用任务队列来处理发送邮件等耗时操作，任务队列使用的是celery库。